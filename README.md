LIMITLESS Prototype
===================

This is a minimal Vite + React prototype for the LIMITLESS app (JJ / Jesse Jarvis).
Run locally:
1. Install dependencies:
   npm install
2. Run dev server:
   npm run dev
3. Open the local URL (usually http://localhost:5173)

This package is ready to upload to Vercel for instant hosting (Upload zip or connect GitHub).

